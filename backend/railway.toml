[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn hybrid_main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[deploy.healthCheck]]
path = "/health"
port = $PORT

[env]
PYTHON_VERSION = "3.11"
